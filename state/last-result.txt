Completed `mcp-resource-prompt-domain-schema`.

Changed:
- Added Prisma schema/migration for MCP resources, resource templates, prompts, prompt messages/arguments, and completion candidates.
- Added `lib/mcp-fixtures` domain types, validators, and services.
- Added idempotent seed defaults for one resource, one template, and two prompts.
- Added reset cleanup for the new fixture tables.
- Added unit coverage in `tests/unit/mcp-resources-prompts.test.ts`.
- Updated task progress, generated DB schema docs, and `state/last-result.txt`.

Required gates passed:
- `npm run lint`
- `npm run typecheck`
- `npm run test:unit -- tests/unit/mcp-resources-prompts.test.ts`
- `npm run db:prepare`

Exit criteria appear satisfied. Runtime handlers, UI, OAuth resource/prompt permissions, and subscriptions remain untouched for later tasks. Note: the repo already had unrelated dirty/untracked planning/doc state when I started; I left that intact.