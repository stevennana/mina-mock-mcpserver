Implemented OAuth Bearer enforcement for MCP and REST.

What changed:
- Added Bearer JWT parsing/validation in `lib/auth/oauth.ts`.
- Added `/mcp/oauth`, Bearer precedence on `/mcp`, and Bearer-filtered MCP/REST tool lists.
- Denied permitted-token calls now return `403`; invalid/expired/revoked Bearer tokens return `401`.
- Added unit coverage and `@oauth-permissions` E2E coverage.
- Updated specs, security/product docs, active task progress log, and `state/last-result.txt`.

Required checks passed:
- `npm run test:unit`
- `npm run test:e2e -- --grep @oauth-permissions`
- `npm run verify`

Exit criteria appear satisfied. Nothing remains for this task.