Implemented the remaining `issued-token-ui-revocation` hardening and wrote the operator handoff to [state/last-result.txt](/Users/stevenna/WebstormProjects/mina-mock-mcpserver/state/last-result.txt).

What changed:
- Added persisted `issuer` metadata for issued tokens via Prisma migration.
- Stored issuer during both OAuth token issuance paths.
- Reconstructed token detail claims from stored issuer metadata, so `claims.iss` matches the issued JWT.
- Tightened Bearer validation to compare stored issuer with JWT `iss`.
- Added deterministic unit coverage for expired-token filtering and issuer reconstruction.
- Updated DB schema docs and the active task progress log.

Required checks all passed:
- `npm run test:unit`
- `npm run test:e2e -- --grep @token-revocation`
- `npm run verify`

Exit criteria appear satisfied. Nothing remains for this task.