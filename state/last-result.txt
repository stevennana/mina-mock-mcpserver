Implemented `mcp-runtime-tools-prompts-json-rpc`.

Changed:
- Added package-owned dispatch for `tools/list`, `tools/call`, `prompts/list`, `prompts/get`, and `completion/complete`.
- Preserved raw tool-call outcomes via an explicit raw message result type.
- Added focused runtime package tests for success, tool error, raw outcomes, delegated prompt validation, completion, provider errors, and unsupported optional methods.
- Updated the active task progress log and wrote `state/last-result.txt`.

Required checks all passed:
- `npm run lint`
- `npm run typecheck`
- `npm run mcp-runtime:build`
- `npm run mcp-runtime:test`

Exit criteria appear satisfied. Nothing remains for this task.