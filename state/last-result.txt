Implemented `mcp-initialize-and-tools-list`.

Added stateless no-auth MCP handlers for `/mcp` and `/mcp/none`, covering `initialize`, `notifications/initialized`, and `tools/list`. Tool listing now reads enabled endpoints through the endpoint domain service and uses the shared generated MCP `inputSchema`; disabled endpoints are hidden. GET/DELETE return deterministic `405` with `Allow: POST`.

Updated docs and the task progress log, and wrote the operator handoff to `state/last-result.txt`.

Checks passed:
- `npm run test:unit`
- `npm run test:e2e -- --grep @mcp-initialize-list`
- `npm run verify`

Reference used for unsupported Streamable HTTP GET/DELETE behavior: official MCP transport spec, https://modelcontextprotocol.io/specification/2025-06-18/basic/transports.