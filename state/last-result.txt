Implemented `root-reset-defaults`.

What changed:
- Added root-protected reset API/UI at `/api/reset` and `/reset`.
- Reset requires root password plus exact `RESET DEFAULTS` confirmation.
- Reset clears current endpoint data, recreates deterministic endpoint seed defaults, and keeps audit history.
- Added non-secret audit evidence for reset failures and success.
- Added unit coverage and `@root-reset` E2E coverage.
- Serialized Playwright E2E workers to avoid shared SQLite reset/mutation races.

Required checks passed:
- `npm run db:prepare`
- `npm run test:unit`
- `npm run test:e2e -- --grep @root-reset`
- `npm run verify`

I also updated `state/last-result.txt` with the operator handoff. Exit criteria appear satisfied; no known remaining work for this task.