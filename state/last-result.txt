Implemented `mcp-runtime-resources-json-rpc`.

Changed the reusable runtime package to handle `resources/list`, `resources/read`, `resources/templates/list`, `resources/subscribe`, and `resources/unsubscribe`; added optional offset pagination helpers; tightened capability advertisement around optional resources/subscriptions; and added focused package tests. Updated the active task progress log and wrote the operator handoff to `state/last-result.txt`.

Required checks passed:

- `npm run lint`
- `npm run typecheck`
- `npm run mcp-runtime:build`
- `npm run mcp-runtime:test`

Exit criteria appear satisfied. Nothing remains for this task.